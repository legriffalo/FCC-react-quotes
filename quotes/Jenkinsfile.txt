pipeline {
  agent any
  tools {nodejs "node"}
  stages {
    stage('Build') {
      steps {
        git 'https://github.com/legriffalo/FCC-react-quotes.git'
        bat 'npm install npm install'
      }}}}